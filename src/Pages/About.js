import React from 'react';
// import PropTypes from 'prop-types';

const About = props => {
    return (
        <div>
           this is about page! 
        </div>
    );
};

// About.propTypes = {
    
// };

export default About;